{
  "$schema": "https://raw.githubusercontent.com/ow-mods/owml/master/schemas/config_schema.json",
  "enabled": true,
  "settings": {
    "Dialogue": {
        "type": "selector",
        "value": "Enabled",
        "options": [
            "Enabled",
            "Enabled (Silent)",
            "Disabled"
        ]
    }
  }
}
